{{#if admin_register}}
<!-- admin_register -->
<div class="admin_register">
  <h1>註冊帳號</h1>
  {{> message}}
  <form action="">
    <div>
      <label for="cpy_no">公司代號</label>
      <input type="text" id="cpy_no" name="cpy_no" class="form-control" placeholder="請輸入公司代號或統編" value="{{ cpy_no }}" required>
    </div>

    <div>
      <label for="cpy_name">公司名稱</label>
      <input type="text" id="cpy_name" name="cpy_name" class="form-control" placeholder="請輸入公司名稱" value="{{ cpy_name }}" required>
    </div>

    <div>
      <label for="email">信箱</label>
      <input type="email" id="email" name="email" class="form-control" placeholder="請輸入信箱" value="{{ email }}" required>
    </div>

<!--
    <div>
      <label for="industry_no">產業類別</label>
      <select name="industry_no" id="industry_no" class="form-control" required>
        <option value="" selected hidden>請選擇</option>
        {{#each industryInfo}}
        <option value="{{this.INDUSTRY_ID}}" {{#unless ../isAdmin}} {{#is this.INDUSTRY_ID 0}} hidden disabled {{/is}} {{/unless}} 
        {{#is this.INDUSTRY_ID ../industry_no}} selected {{/is}}>
        {{this.INDUSTRY_NAME}}
        </option>
        {{/each}}
      </select>
    </div>
-->

    {{#if isAdmin}}
    <div>
      <label for="ishr">客服使用者</label>
      <select name="ishr" id="ishr" class="form-control" required>
        <option value="" selected>請選擇</option>
        <option value="1" {{#is ishr '1'}} selected {{/is}}>是</option>
        <option value="0" {{#is ishr '0'}} selected {{/is}}>否</option>
      </select>
    </div>

    <div>
      <label for="ishr">權限</label>
      <select name="isadmin" id="isadmin" class="form-control" required>
        <option value="" selected>請選擇</option>
        <option value="1" {{#is isadmin '1'}} selected {{/is}}>管理員</option>
        <option value="0" {{#is isadmin '0'}} selected {{/is}}>一般使用者</option>
      </select>
    </div>
    {{/if}}

    <div>
      <label>輸入密碼</label>
      <input type="password" id="password" name="password" class="form-control" placeholder="請輸入密碼" value="{{ password }}" required>
    </div>

    <div>
      <label>確認密碼</label>
      <input type="password" id="confirmPassword" name="confirmPassword" class="form-control" placeholder="請再次輸入密碼" value="{{ confirmPassword }}" required>
    </div>

    <div class="formButton">
      <input id="cancel" type="button" class="btn btn-primary" onclick="Method.common.page('/admin_company')" value="返回">
      <input id="add" type="submit" class="btn btn-info" value="新增">
    </div>
  </form>
  {{#unless isAdmin}}
  
  {{/unless}}
</div>
{{/if}}